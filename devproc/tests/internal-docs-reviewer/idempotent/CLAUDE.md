# Widget Tracker — Project Guide

## Current status

**No feature currently in progress.**

`csv-export` completed 2026-05-02 — added CSV export to the reports page (#47).

## Feature model

Features are tracked under `features/`; completed ones are described in
`features/COMPLETED.md`. This file's **Current status** section is
high-level status only — the feature in progress, plus at most one line for
the most recent completion. Older completions live in `features/COMPLETED.md`
only, not here.
