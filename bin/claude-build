#!/bin/bash
set -euo pipefail

REPO="$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)"

docker build -t claudedev \
    --build-arg UID="$(id -u)" \
    --build-arg GID="$(id -g)" \
    -f "$REPO/docker/Dockerfile" \
    "$REPO"
