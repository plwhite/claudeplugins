# Claude plugins

Various useful plugins.

- `demo` - a demo plugin with a `demo-skill` skill and a `demo-agent` agent.
- `devproc` - dev process skills for managing features (init, create, start, checkpoint, end).
